# Broken root plans — archived, NOT re-derived

These `plans/2026-09-*-bigfile-Phonics-Fun-AudioManager.java.md` files were
auto-generated by `cron_refactor_planner.py` and reviewed as **NEEDS-REVISION**:

- **Language mismatch**: target is `app/src/main/java/.../AudioManager.java`
  (Java/Android), but every objective prescribes TypeScript tooling
  (`constants.ts`, `index.ts` barrels, `pnpm dlx knip`, `pnpm run build`).
- **Zero structural grounding**: the review pass found `objectives=0` against
  the live file; the 12 "objectives" are template filler (8 verbatim
  "Hardening pass N" entries).
- **No `.impl`/`.review` verdict with `ready_for_impl: true`** — the only
  recorded verdict is `NEEDS-REVISION`.

Resolution: **archived, not implemented.** The file is already the largest
source file in the repo; a real refactor plan would need to be authored from
the Java file's actual structure, not a TS-shaped template. Re-deriving would
fabricate work. Awaiting user direction before any refactor.
