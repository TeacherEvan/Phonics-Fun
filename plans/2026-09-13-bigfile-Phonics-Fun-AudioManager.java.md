# Refactor plan — `app/src/main/java/com/phonicsfun/core/AudioManager.java` in `TeacherEvan/Phonics-Fun`

> Auto-generated by `cron_refactor_planner.py` on 2026-09-13T03:52:26.376405+07:00.
> v2 — file-aware: objectives are derived from structural analysis (exports, hooks,
> render branches, repeated literals, import cross-refs), NOT a generic N-slice filler.

## File header

| field | value |
|---|---|
| repository | `TeacherEvan/Phonics-Fun` |
| path | `app/src/main/java/com/phonicsfun/core/AudioManager.java` |
| size | 14.4 KB (14,785 bytes) |
| lines | 408 |
| generated | 2026-09-13T03:52:26.376425+07:00 |

### Top imports (sample)
  - `import android.content.Context;`
  - `import android.media.AudioAttributes;`
  - `import android.media.MediaPlayer;`
  - `import android.media.SoundPool;`
  - `import android.speech.tts.TextToSpeech;`
  - `import android.util.Log;`
  - `import java.util.HashMap;`
  - `import java.util.Locale;`
  - `import java.util.Map;`

## Structural analysis

**Repeated literals (≥2×):**
- `) || word.equalsIgnoreCase(` (×6)
- `, soundPool.load(context, getResId(` (×3)

## Why this file matters

This is the largest source file under the standard source roots
(`src, app, lib, convex, components, pages, src-tauri/src`) in `Phonics-Fun`. The objectives below are
derived from the structural signals above — they name concrete extractions
(line-anchored, symbol-anchored) rather than generic slice-N placeholders.

## Objectives (12 tickable)

### OBJ-001 — Hoist repeated literal `') || word.equalsIgnoreCase('` (×6) to a constant
- **Target:** String literal appears 6× in `app/src/main/java/com/phonicsfun/core/AudioManager.java`. Extract to
  `./constants.ts` (already exists per imports) or co-located `strings.ts`.
- **Why:** Repeated literals indicate copy that should be localizable or reused.
- **Acceptance:** zero occurrences of the literal in `app/src/main/java/com/phonicsfun/core/AudioManager.java` after the change;
  constant referenced via the barrel.
- **Validation:** `rg -F '<literal>' app/src/main/java/com/phonicsfun/core/AudioManager.java` returns 0.
- **Evidence:** grep before/after.

### OBJ-002 — Run knip / ts-prune against the page after extraction
- **Target:** After OBJ-001 → OBJ-006 land, some imports in `app/src/main/java/com/phonicsfun/core/AudioManager.java` will be unused.
- **Tool:** `pnpm dlx knip --reporter compact` or `pnpm dlx ts-prune`.
- **Acceptance:** zero unused exports; zero dead imports in `app/src/main/java/com/phonicsfun/core/AudioManager.java`.
- **Validation:** `rg -n '^import' app/src/main/java/com/phonicsfun/core/AudioManager.java` cross-checked against actually-used symbols.
- **Evidence:** knip output diff before/after.

### OBJ-003 — Audit sibling `index.ts` barrels for circular / stale re-exports
- **Target:** `app/src/main/java/com/phonicsfun/core/components/index.ts` (or its peer in
  any newly split directory).
- **Acceptance:** barrel exports only symbols whose source file exists.
- **Validation:** `rg -n 'export' barrel.ts | wc -l` matches live exports.
- **Evidence:** barrel diff.

### OBJ-004 — Reduce `app/src/main/java/com/phonicsfun/core/AudioManager.java` below 204 lines via cumulative extractions
- **Target:** After OBJ-001 → OBJ-008 land, the remaining render block should
  be a thin composition of named sub-components.
- **Goal:** `app/src/main/java/com/phonicsfun/core/AudioManager.java` shrinks from 408 to ≤204 lines.
- **Acceptance:** `wc -l` shows the reduction; behavior unchanged via tests + e2e.
- **Validation:** `pnpm run build && pnpm run test && pnpm run test:e2e` (full gate).
- **Evidence:** before/after line count + green build.

### OBJ-005 — Hardening pass 5
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-006 — Hardening pass 6
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-007 — Hardening pass 7
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-008 — Hardening pass 8
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-009 — Hardening pass 9
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-010 — Hardening pass 10
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-011 — Hardening pass 11
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

### OBJ-012 — Hardening pass 12
- **Target:** Tighten types / tighten prop drilling in `app/src/main/java/com/phonicsfun/core/AudioManager.java` post-extraction.
- **Acceptance:** stricter types where the inference is loose; no `any` added.
- **Validation:** type-check.
- **Evidence:** type diff.

## Definition of Done

- [ ] Every OBJ-00X above has a Definition-of-Done entry below it filled in by the IMPLEMENT pass.
- [ ] The original `app/src/main/java/com/phonicsfun/core/AudioManager.java` either disappears (split into smaller modules) or shrinks materially while preserving behavior.
- [ ] `pnpm run type-check && pnpm run lint && pnpm run test && pnpm run build` is green (Next.js full gate).
- [ ] `git diff --stat` shows only the planned paths.
- [ ] `debrief.md` (per surgical-implementation) records evidence per objective.

## Security notes

- This plan modifies source under `Phonics-Fun`. No credential rotation, no
  permission changes, no production deploy. APPROVAL_GATE = REVIEW.
- `git push` will target the same branch the cronjob cloned from; if push is
  rejected the plan remains in the working tree and the next tick retries.

## Provenance

- planner: `cron_refactor_planner.py` (v2 — file-aware objectives)
- plan filename: `2026-09-13-bigfile-Phonics-Fun-AudioManager.java.md`
- reviewer: `cron_plan_reviewer.py` will append a `## REVIEW <iso>` section on
  its next tick.
- implementer: `cron_surgical_impl.py` will pick this plan up once the reviewer
  marks it `READY` or `READY-WITH-WARNINGS`.


## REVIEW 2026-09-13T04:00:13.564800+07:00

**Verdict:** `NEEDS-REVISION`

**Structural check:** objectives=12 file_header=✓ imports=✓ why=✓ dod=✓ security=✓

**Gaps:**
1. **Language/tooling mismatch.** The target is a Java file (`AudioManager.java`) but every objective references TypeScript tooling: `.ts` files, `knip`/`ts-prune`, `index.ts` barrels, `pnpm`. None of these apply to Java. The structural analysis extracted Java literals (`) || word.equalsIgnoreCase(`) but then prescribed TypeScript-specific extractions. The entire objective set is invalid for the file type.
2. **Boilerplate filler objectives.** OBJ-005 through OBJ-012 are eight identical "Hardening pass N" entries with no differentiated targets, no line/symbol anchors, and no concrete acceptance criteria beyond "type-check." These are template-filler, not derived from structural analysis — exactly what the v2 generator claims to avoid.
3. **Missing required plan fields.** Structural check reports `has_header=None`, `has_imports=None`, `has_why=None`, `has_dod=None`, `has_security=None`. The plan lacks a proper header block, import cross-reference analysis, definition of done, and security section — all required by the G&L Auditor V2 standard. The "Why this file matters" section is a single generic sentence with no file-specific rationale.
